# Быстрый запуск передачи по API

## ✅ ШАГ 1: На ведомом узле (192.168.1.1)

Откройте **ДВА терминала**:

### Терминал 1 - Основное приложение:
```bash
cd /path/to/CRSF
sudo ./crsf_io_rpi
```

**Примечание:** Для режима без телеметрии используйте:
```bash
sudo ./crsf_io_rpi --notel
```

### Терминал 2 - API интерпретатор:
```bash
cd /path/to/CRSF
./crsf_api_interpreter [порт] [IP_API_сервера] [порт_API_сервера]
```

**Примеры:**
```bash
# Локальный режим (без телеметрии)
./crsf_api_interpreter 8082

# Удаленный режим (с телеметрией)
./crsf_api_interpreter 8082 192.168.1.50 8081
```

**Важно:** Для работы телеметрии через API необходимо указать адрес API сервера.

---

## ✅ ШАГ 2: На ведущем узле

Запустите API сервер:
```bash
cd /path/to/CRSF
./crsf_api_server [порт] [IP_ведомого] [порт_ведомого]
```

**Пример:**
```bash
./crsf_api_server 8081 192.168.1.1 8082
```

**Или используйте скрипт:**
```bash
./start_api_server.sh 8081 192.168.1.1 8082
```

Проверить статус:
```bash
ss -tlnp | grep 8081
pgrep -f crsf_api_server
```

---

## ✅ ШАГ 3: Отправка команд

### Вариант A: Через Python скрипт
```bash
cd /home/dmitriy/Загрузки/CRSF
python3 example_api_usage.py
```

### Вариант B: Через Python интерактивно
```bash
python3
>>> from api_wrapper import CRSFAPIWrapper
>>> crsf = CRSFAPIWrapper("http://localhost:8081")
>>> crsf.auto_init()
>>> crsf.set_work_mode("manual")
>>> crsf.set_channel(1, 1500)
>>> crsf.send_channels()
```

### Вариант C: Через графический интерфейс
```bash
cd /path/to/CRSF
python3 crsf_realtime_interface.py --api --api-url http://localhost:8081
```

**Примечание:** Телеметрия будет доступна, если API интерпретатор запущен с указанием адреса API сервера (см. ШАГ 1).

---

## 🔍 Проверка работы

### Проверить что команды доходят до ведомого узла:
На ведомом узле (192.168.1.1):
```bash
tail -f /tmp/crsf_command.txt
```

### Проверить что API сервер работает:
```bash
python3 -c "import requests; print(requests.get('http://localhost:8081/').status_code)"
```

---

## 📋 Чек-лист запуска

- [ ] На ведомом узле запущен `crsf_io_rpi`
- [ ] На ведомом узле запущен `crsf_api_interpreter` (с указанием адреса API сервера для телеметрии)
- [ ] На ведущем узле запущен `crsf_api_server`
- [ ] Можно отправлять команды через API
- [ ] Телеметрия доступна (если интерпретатор настроен на отправку телеметрии)

---

## 🛑 Остановка

### Остановить API сервер:
```bash
pkill -f crsf_api_server
```

### Остановить API интерпретатор:
```bash
pkill -f crsf_api_interpreter
```

### Остановить основное приложение:
Нажмите `Ctrl+C` в терминале где запущен `crsf_io_rpi`

